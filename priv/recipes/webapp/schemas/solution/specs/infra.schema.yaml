name: infra
description: "Infrastructure specifications"
artifact_type: infra
fields:
  name:
    type: string
    required: true
  type:
    type: enum
    values: [compute, storage, cdn, dns, ci, monitoring, auth]
  provider:
    type: string
  config:
    type: map
  serves:
    type: list
    item_type: ref
    ref_to: domain.architecture
